# tweet-wakatime-stats

## usage

### upload lambda function

First, configure aws cli.

```bash
aws configure
```

Second, upload src directory to aws lambda function endpoint.

```bash
./upload.sh
```
